{
  "name": "payprecision-server",
  "version": "1.0.0",
  "description": "PayPrecision backend — Express + SQLite (node:sqlite built-in)",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
